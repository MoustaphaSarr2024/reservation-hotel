option_settings:
  aws:elasticbeanstalk:application:environment:
    NODE_ENV: production

container_commands:
  01_run_migrations:
    command: "node -e \"const {sync} = require('./src/models/indexModels'); sync().then(() => console.log('Migrations terminées')).catch(e => {console.error(e); process.exit(1)});\""
    leader_only: true
